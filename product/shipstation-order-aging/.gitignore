# --- Generated output ---
# Contains real order/customer data pulled from ShipStation. Only commit a
# specific snapshot deliberately with `git add -f`.
fulfillment_dashboard.html
*.log

# --- Secrets ---
# ShipStation API key/secret + Gmail app password for the unattended weekly
# report. Real values live only in config.json on this machine; the
# committed config.example.json has placeholders only.
config.json

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
env/

# --- Editor & OS ---
.vscode/
.idea/
.DS_Store
Thumbs.db
